# AGENTS.md

## 작업 목표

이 저장소는 한국어 LLM을 단계적으로 개발하는 프로젝트다. 당장의 우선순위는 100M 모델 학습이 아니라, 이미 실행한 40M 한국어 위키백과 파일럿의 산출물·생성 결과·평가 점수를 확인하고 다음 실험을 결정하는 것이다. 파일럿 기록은 `experiments/pilot-kowiki-2026-09-18/README.md`와 관련 문서에서 확인한다.

## 사용자와 소통하는 방법

사용자는 LLM 개발 초보자다. 작업을 진행할 때 매 단계마다 다음을 짧고 분명하게 설명한다.

1. 지금 하려는 일
2. 왜 필요한지
3. 나오는 용어와 명령의 뜻
4. 실행할 명령 또는 변경 내용
5. 예상 결과와 실제 결과
6. 결과에 따라 다음에 할 일

명령어와 설정값을 제시할 때는 복사해 실행할 수 있게 쓰고, 성공·실패를 판단하는 기준도 함께 설명한다. 모르는 값이나 확인하지 못한 결과를 추측하지 않는다. 데이터 출처, 토큰 수, 체크포인트, 평가 점수는 기록과 실제 파일을 구분해 보고한다.

## 모델 사용 규칙

- 기획, 개념 설명, 아키텍처 비교, 결과 검토와 같은 읽기·판단 작업은 GPT Sol 또는 GPT Astra를 사용할 수 있다.
- 코드, 문서, 설정, 스크립트, 테스트, 데이터 파이프라인을 수정하거나 생성하는 구현 작업은 반드시 GPT Luna의 `xhigh` 추론으로 수행한다.
- 학습·평가·전처리·토크나이저 생성처럼 명령 실행으로 파일, 체크포인트, 로그 또는 환경 상태를 바꾸는 작업도 구현 범위로 보고 Luna `xhigh`를 사용한다. 단순한 읽기 전용 탐색 명령은 현재 작업 단계에 맞는 모델로 수행할 수 있다.
- `AGENTS.md`만으로 모델 전환을 강제할 수는 없다. 실제 런타임이 모델을 바꿀 수 있는지 확인하고, 구현을 시작하기 전에 Luna `xhigh`가 적용되었는지 사용자에게 투명하게 알린다. 적용할 수 없으면 다른 모델로 구현을 조용히 진행하지 말고 제한을 보고한다.
- 토큰을 아끼기 위해 독립적인 읽기 작업은 묶어서 수행하고, 같은 파일·명령·설명을 반복하지 않는다. 필요한 만큼만 계획과 검증을 한다.

## 작업 원칙

- 먼저 현재 파일, 실행 기록, Git 상태를 읽고 기존 사용자 변경을 보존한다.
- 사용자가 요청한 범위 밖의 파일은 수정하지 않는다. 특히 학습을 요청받지 않은 상태에서 본 학습을 시작하지 않는다.
- 삭제, 덮어쓰기, 대규모 데이터 이동, 기존 체크포인트 변경처럼 되돌리기 어려운 명령은 대상과 영향을 먼저 확인하고 명시적인 요청 없이 실행하지 않는다.
- 구현 후에는 관련 정적 검사·테스트·드라이런·로그 확인 등 가장 작은 충분한 검증을 수행하고, 무엇을 검증했는지와 남은 한계를 기록한다.
- GPU, Python, PyTorch, 데이터 버전과 같은 환경 의존 값은 결과와 함께 기록한다. 다른 머신의 수치와 섞어 비교하지 않는다.
- 사용자가 이해할 수 있도록 중요한 결정에는 이유를 붙인다. 전문 용어는 처음 나올 때 한 문장으로 풀어 쓴다.

## 기본 진행 순서

1. 파일럿 기록과 실제 산출물의 존재 여부를 확인한다.
2. 체크포인트로 짧은 생성 예제를 만들고, 생성 품질과 알려진 평가 점수를 함께 확인한다.
3. 기록과 실제 결과가 다르면 차이를 먼저 정리한다.
4. 데이터·토크나이저·학습 설정·자원 예산을 확인한 뒤 다음 규모의 실험을 계획한다.
5. 계획이 확정된 후에만 구현과 학습을 진행한다.

